image: debian:trixie

stages:
  - build

build-job:
  stage: build
  before_script:
    - apt-get update
    - apt-get install -y git cmake ninja-build build-essential libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libxkbcommon-dev libwayland-dev libfreetype6-dev
  script:
    - cmake --version
    - ninja --version
    - cmake -G Ninja -DCMAKE_CXX_COMPILER=g++ -DCMAKE_C_COMPILER=gcc -DCMAKE_BUILD_TYPE=Release -B build -S .
    - cmake --build build -j$(nproc)
  artifacts:
    paths:
      - build/
